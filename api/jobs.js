const config = require('../config/config.js');
const errors = require('../config/errors.js');

const Jobs = module.exports = {
    //every second
    '* * * * * *': async () => {
    },

    //every minute
    '* * * * *': async () => {
    },
};